<table>
    <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= entry.name %></td>
        <td><%= entry.entrybody %></rd>
        <td><%= link_to '詳細', entry %></td>
      </tr>
    <% end %>
    <tbody>
</table>
<p1><%= link_to '新規作成', new_entry_path %></p1>
